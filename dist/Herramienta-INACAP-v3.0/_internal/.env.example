# ============================================================================
# CONFIGURACION DE BASE DE DATOS - ARCHIVO DE EJEMPLO
# ============================================================================
#
# IMPORTANTE: Este archivo es SOLO una referencia (.env.example)
#
# Para usar la aplicacion:
# 1. Copia este archivo como ".env" (sin el ".example")
# 2. Rellena los valores segun tu configuracion
# 3. NO SUBAS .env al repositorio (ya esta en .gitignore)
#
# ============================================================================

# CONFIGURACION DE BASE DE DATOS (HÍBRIDO)
# La aplicación usa MySQL directo para readers/batch
# En futuro, usará API Gateway para operaciones GUI

DB_HOST=localhost
DB_PORT=3306
DB_NAME=inacap_db
DB_USER=inacap_user
DB_PASSWORD=inacap_password

# CONFIGURACIÓN FUTURA - API GATEWAY (descomenta cuando esté disponible)
# API_URL=https://xxxxx.execute-api.sa-east-1.amazonaws.com/prod/consultar
# API_TIMEOUT=30
