# Configuración para uso en RED CORPORATIVA (con túnel SSH)
# Renombra este archivo a .env cuando uses la red corporativa

DB_HOST=localhost
DB_PORT=3307
DB_NAME=inacap_test
DB_USER=admin
DB_PASSWORD=Base_de_datos_Inacap2026
